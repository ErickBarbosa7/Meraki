{
  "framework": "vite",
  "buildCommand": "npm run build -w frontend",
  "installCommand": "npm ci",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}