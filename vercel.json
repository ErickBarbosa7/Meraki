{
  "framework": "vite",
  "buildCommand": "npm run build -w frontend",
  "installCommand": "npm ci --include=dev",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}