# URL absoluta de la API backend (solo cuando el frontend NO está en el mismo origen).
# En desarrollo se ignora (usa el proxy /api"). Ajusta con tu URL de Render, ej:
# VITE_API_URL=https://meraki-api.onrender.com
VITE_API_URL=