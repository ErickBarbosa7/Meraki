# Origen del backend (SIN /api al final; la app agrega "/api" sola y también tolera si incluyes /api).
# Solo aplica en producción (Vercel/Render). En desarrollo se ignora (usa el proxy local).
# Ejemplo:
# VITE_API_URL=https://meraki-reth.onrender.com
VITE_API_URL=